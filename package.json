{
  "name": "restaurantsearcher",
  "version": "1.0.0",
  "description": "- Hot Pepper",
  "scripts": {
    "start": "npm-run-all --parallel serve-http serve-https",
    "serve-http": "live-server --port=3000",
    "serve-https": "live-server --https=--cert=localhost.pem --key=localhost.key --port=3001"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
