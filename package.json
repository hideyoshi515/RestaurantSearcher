{
  "name": "restaurantsearcher",
  "version": "1.0.0",
  "description": "- Hot Pepper",
  "scripts": {
    "start": "live-server --https=--cert=localhost.pem --key=localhost.key --port=3001"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
